# BFB MVP progress

Updated: 13 September 2026

Status: Blocked — D01 discussion records done; Mac unlocked, L05 Terminal automation still denied

Plan: [Remote launch and agent discussion MVP](mvp.plan.md)

## Current checkpoint

- Timo explicitly authorized Codex to finish the full local MVP independently on autopilot, without routine approval pauses, and instructed work to resume. This authority is recorded in the plan's Goal section. Feature branch: `codex/remote-launch-discussion-mvp`, based on `main` at `e1bc7a0`.
- Plan updated; ADR 0002 accepted; D01–D03 assigned with dependency edges, test targets and evidence paths. Roadmap generation now covers 45 packages.
- L01, the daemon/CLI kernel, is done under ADR 0003's approved local scope. Its exact target, uncached native tests, full repository verification, IC-1 and clean-worktree check passed at `f19d188`; evidence is committed.
- L02 implements local checkout link/list/verify/unlink, immutable filesystem/Git identity, path-free summaries and tightening-only repository policy. Clean certification passed at `0c436be`, including its exact target, full verification and clean-worktree assertion; the bounded evidence accompanies its done status.
- L03's provider kit and synthetic lifecycle passed initial clean certification at `367ede9`: immutable probes/plans, capability ceilings, exact discussion identity, provider-local registration and approved setup/doctor transactions. The additive interactive-resume correction passed exact L03 and full clean-checkout certification at `32f1354`; its evidence is committed before L05 consumes it. Real provider descriptors remain discovery-only; their tracked capabilities require L07/P01 certification.
- C06 implements per-workspace P-256 public keys, private-by-default named-human/project grants, action-bound step-up, hashed single-use challenges, request-bound possession authentication and token/grant epoch fences. Clean certification passed at `0247574`, including real Worker races, browser non-UV assertions and the full browser regression. Bounded evidence is committed with its done status.
- L08 clean certification passed at `18b7e20`: its exact target, full verification, C06, IC-1, real hub and D1 migration regressions, Linux cross-build and clean-worktree assertion. Native signed ACL/TLS, browser passkey pairing, two-workspace launchd/Worker channels, lost nudges, hibernation, wake, expiry alarms, crash recovery and live revocation are covered. Bounded evidence records the development-signing and whole-machine-sleep limitations; continuing with L04.
- L04 clean certification passed at `c1653e6`: exact `pnpm test:l04`, full repository verification, Linux cross-build and the final clean-worktree assertion. The signed native gate proves a real fixed-helper Terminal receipt, three daemon-driven app relaunch cycles, opaque wake identity and notification-denied behavior. Native visual review and its copy/accessibility corrections are complete. Bounded evidence records the earlier consent-denied and timeout observations without treating them as final success. L04 is done; continuing with C09 before L05 can consume the launch contracts.
- C09 clean certification passed at `e22e2cf`: exact `pnpm test:c09` (247 protocol tests with Go parity, 71 focused tests, real WebSocket/Worker/D1 races and migration checks), full verification (588 TypeScript tests, Go and 11 Swift tests), C01/D1/C07/C08/C06/IC-1 regressions, Linux cross-build and final clean-worktree assertion. Bounded evidence records immutable launches/snapshots, fenced occupancy, final reauthorization, controls, exact-session resume, verifier-only wake and containment recovery. C09 is done; L05 now owns the actual local supervisor, process inspection and lock proof.
- L01 implements private socket/CLI dispatch, SQLite WAL migrations/recovery, redacted logs, credential interfaces and per-user launchd installation, including real process restart, launchd and Swift-to-UDS tests. The genuinely fresh-account environment check remains untested and owned by G02. No production action has been taken.
- L05 implements native supervision, cleanup, lease renewal, durable controls, bound signal delivery and exact-session child-launch integration. The resume checkpoint passed exact L03 (333 protocol cases and the provider race suite), the complete supervisor race suite, signed-helper authentication, full repository verification (680 TypeScript tests, Go and 11 Swift tests) and Linux executable/test compilation in the implementation checkout. Resume requires complete prior native release evidence, an immutable C09 child, fresh source absence and the ordinary physical-lock/final-authorization gates. The native tests inject signing/cloud observations at explicit boundaries; the daemon never signals directly.
- Exact-tab focus delivery is implemented with fresh native TTY/lock/group checks, a one-way control barrier and signed-app per-effect reauthorization. Focused native race/PTY tests, 351 protocol tests with Go parity and 15 Swift tests pass. The signed app's real focus gate reached a locked macOS session and correctly refused Terminal work; real Apple-event focus acceptance is still unverified. The production entry point now connects the launch/control consumers, signed-app bridge, bound runner transport and fixed helper commands, including bounded wake-hint redemption that only triggers a durable command pull. Reproduced controls stranded by an absent local assignment now settle only from bound terminal receipts, without inventing native outcomes. Wiring, wake, daemon restart and control regressions, the complete native race suite, full repository verification (698 TypeScript tests, Go and 15 Swift tests), and Linux executable/test compilation pass. Explicit local recovery now has a signed-helper-only CLI/RPC and certifies complete retained native release history without clearing uncertainty or fabricating events. Focused recovery/CLI/binary race tests, real signed-helper authentication, 359 protocol tests with Go parity, full verification (706 TypeScript tests, Go and 15 Swift tests) and Linux builds pass. Complete real signed Terminal supervision acceptance remains pending. L05 and the full MVP are not complete.

## Milestones

The signed L05 integration harness now exercises the full local helper chain with
a synthetic C09 transport, without substituting native identity checks. Its PTY
diagnostic passes exact cwd/Git/argv/environment checks, duplicate bound interrupt,
surviving-child heartbeat and occupancy, sticky escape, signed explicit recovery,
PID-reuse protection and confirmed cloud release. Full native race tests,
repository verification (706 TypeScript tests, Go and 15 Swift tests) and Linux
executable/test compilation also pass. Those are implementation-checkout results,
not real Terminal acceptance. The resumed 13 September check finds the Mac GUI
available, but the computer-control tool explicitly denies Terminal access for
safety reasons. L05 remains blocked on real focus and Ctrl-C/close coverage and
subsequent clean package certification. D01 completed separately from its
already-done prerequisites; no package may consume unfinished L05.

D01 clean certification passed at `5856a93`. It implements discussion-purpose runs, frozen briefs/rosters, bounded turns,
immutable session binding, distinct delivery states, attributed recommendations,
human intervention/cancellation/decisions and filtered participant reads. Its
human API exposes no participant-authority override. Exact `pnpm test:d01` passes
from a clean detached checkout: 421 protocol tests with Go parity and typed round
trips, 111 focused tests (including 31 discussion-domain and five mounted browser
tests), and real D1/WorkspaceHub checks. Two independent Workers prove duplicate
creation, stale-edit/dispatch/decision races, eviction, six bounded turns and late
batch rollback. Empty and populated 0016-to-0017 migrations preserve ordinary
work, snapshots, sessions and event cursors. The 62 synthetic discussion fixtures
are deterministic. Full repository verification passes (804 TypeScript tests,
Go checks and 15 Swift tests), as do exact C08/C09, Linux cross-build and the final
clean-worktree check. The committed evidence manifest marks D01 done without
claiming provider execution. Every remaining package in the approved MVP depends
directly or transitively on L05. The Mac is now unlocked, but permitted Terminal
automation remains unavailable. The remaining interaction harness and exact
clean-checkout gate are not complete; this is separate from BFB's macOS Automation
consent. No unfinished package is consumed and the full MVP is not done.

| Milestone | Packages | State | Completion check |
| --- | --- | --- | --- |
| Trusted Mac | L01–L03, C06, L08, L04 | All milestone packages done | Enroll, register exact checkout, probe providers, reconnect, revoke |
| Remote launch | C09, L05, W02 | C09 done; L05 blocked on Terminal acceptance | Card starts fake provider; contention, expiry, revocation and containment fail safely |
| Real agent work | L06, E01–E02, A01–A04, L07, P01 | Pending — E01 and A01 implemented on their branches with gates and evidence complete; both await L05 and L06 | Claude/Codex start, scoped context, attention, explicit result and human acceptance |
| Discussion | D01–D03 | D01 done; D02/D03 wait on execution prerequisites | Two read-only participants, bounded turns, recovery, intervention, human decision |
| Running local delivery | Integrated MVP | Pending | Start services, full browser/runner/provider smoke, negative checks and health instructions |

## Verification

- Starting repository baseline: 12 completed work packages; previous `pnpm verify` passed with 343 TypeScript tests plus Go and Swift checks.
- The baseline is not evidence for the new MVP features.
- Initial provider discovery found Codex `0.153.4` and Claude Code `2.1.268`. Claude subsequently updated; the live experiment below uses `2.1.269` and does not transfer evidence between versions.
- IC-1 passed in an isolated clean baseline checkout after installing the pinned Chromium build, building workspace packages and rebuilding the declared native SQLite dependency. This includes 14 browser scenarios and the dedicated two-scenario OAuth rerun. The initial environment failures were missing test dependencies, not established product failures.
- Added `pnpm build` before the aggregate IC-1 domain tests so the command does not depend on previously generated package output.
- L01 `pnpm test:l01` passed from a clean checkout with 136 TypeScript protocol tests, Go protocol checks and race-tested daemon/auth/CLI/binary packages, including real subprocess crash/restart, per-user launchd installation and a compiled Swift Unix-socket client. Clean-checkout `pnpm verify` passed 348 TypeScript tests plus Go and Swift/Xcode checks; `pnpm worktree:check` confirmed no tracked drift.
- The updated `pnpm test:ic1` command also passed in the L01 clean checkout at `2ef952a`, including all 14 browser scenarios and the two-scenario dedicated OAuth rerun; the worktree remained clean afterward.
- L02 local acceptance passed: real APFS aliases/case spelling, linked worktrees/common-directory retargeting, directory replacement/removal, dirty/unborn/detached Git states, zero Git metadata writes, policy hash changes, default/pagination/restart behavior, and real CLI/RPC path redaction. Five shared policy fixtures pass through both Go YAML parsing and canonical cloud commands.
- New conditional RPC fixtures exposed a diagnostic-category mismatch: both codecs rejected response paths, but TypeScript lacked Go's generic constant-error mapping. The mapping is aligned, with shared differential regression fixtures; 139 protocol tests pass.
- L02 clean certification at `0c436be` passed `pnpm test:l02` and `pnpm verify` (356 TypeScript tests plus native Go and Swift/Xcode checks). Linux/amd64 cross-build and the final clean-worktree assertion passed. This is checkout registration/revalidation, not yet provider launch.
- L03 live synthetic experiments on Claude Code `2.1.269` and Codex `0.153.4` observed fresh/resumed/forked identity, remembered context, wrong-session rejection and post-turn interrupt. Both process repeated input twice; Claude accepts overlapping writers while Codex rejects an active writer. BFB must own deduplication and fencing. Grok `1.0.25` remains discovery-only.
- The live experiment also killed owned processes before identity observation and after turn observation, without retrying uncertain input. Inherited/peer write and escalation canaries stayed absent. Full read-only/managed-policy certification and native lower-authority external delivery remain unverified and unavailable to production manifests.
- L03 clean certification at `367ede9` passed its exact target (five probe-classification cases, 139 protocol tests and native race tests), full repository verification (356 TypeScript tests, Go and Swift/Xcode), both bounded live provider experiments, Linux cross-build and the final clean-worktree assertion.
- C06 clean certification at `0247574` passed its exact target (147 protocol and 64 focused TypeScript tests, Go protocol checks, real D1/WorkspaceHub races and one browser scenario), full verification (411 TypeScript tests, Go and Swift/Xcode), all 15 browser scenarios and the clean-worktree assertion. Revocation prevents renewal before cleanup; actual live sockets remain L08-owned.
- L08 clean certification at `18b7e20` passed its exact target (159 protocol and 59 focused TypeScript tests, native race/ACL/launchd/Worker tests and browser pairing), full verification (436 TypeScript tests, Go and Swift/Xcode), C06 and IC-1 including all 16 browser scenarios, real hub and D1 migration regressions, Linux cross-build and the clean-worktree assertion.
- L04 core checks pass locally: 185 protocol cases with Go parity, race-tested daemon/app bridge, and 11 Swift tests including every Local RPC golden fixture, malformed links, strict Terminal identity, pairing-origin/workspace binding, consent/locked-state rejection and bounded installer behavior. The signed self-hosted native diagnostic proves kernel-peer verification, real OS custom-link delivery, app-only quit, independent child/storage survival and adversarial Unix-socket handling. These are implementation checks, not the missing full clean-checkout L04 certification.
- L04 full repository verification passes with 462 TypeScript tests, Go checks and 11 Swift tests, including after the provisioning and verified-helper fixes. A repeated locked-session native diagnostic exposed a LaunchServices teardown race: its inventory briefly returned the same app PID after kernel-confirmed process exit. A bounded standalone reproducer confirmed the stale PID; the test probe now requires kernel liveness as well as the exact signed-bundle path. The apparent locked-session relaunch was stale inventory, not a live app.

## Decisions and limitations

- Fresh BFB-owned discussion sessions first; existing live-session invitations deferred.
- An early L03 capability experiment validates continuation, fork, identity, read-only boundaries, cancellation, and native delivery before adapter contracts freeze.
- D1/WorkspaceHub owns business state; the Mac executes; peer text grants no human authority.
- No automatic worktrees, extra room backend, external room dependency, or production deployment.
- Package status changes require the exact clean-checkout gate and committed redacted evidence. Milestone progress does not bypass this rule.
- L01's native lifecycle checks use empty BFB state under the current macOS account. A genuinely new macOS account has not been tested; this is distinct from clean-checkout certification and remains an explicit acceptance limitation.
- Timo approved deferring that specific fresh-account check to G02. ADR 0003 records the exception, preserves the existing safety tests, and forbids describing the release environment check as passed.
- Timo approved BFB's development App ID/profile setup and updated full-autopilot authority on 12 September. Necessary in-scope shared-state actions no longer require repeated rollout confirmation. The BFB Associated Domains App ID and Mac profile have been created, using the existing development certificate and this Mac only. No production deployment was added to the local MVP scope. The exact gate still rejects the self-hosted diagnostic override and a locked interactive session.

## Checkpoint log

- 11 September: began the approved MVP implementation and incorporated the follow-up agent-chat research into the plan.
- 11 September: assigned D01–D03 and DG-01–DG-03; generated roadmap passes with 45 packages. Added a regression for discussion-package generation. `pnpm verify` passed with 344 TypeScript tests, Go checks and Swift/Xcode checks.
- 11 September: committed the plan/roadmap as `8e27776`; F02 prerequisite gate passed (132 TypeScript tests plus Go). Began L01 with isolated local-state, socket, storage, logging, credential-interface and lifecycle tests.
- 11 September: first complete L01 local gate passed; added canonical daemon-status/error fixtures and documented CLI, storage recovery, limits and the native Keychain boundary still owned by L08/L04.
- 11 September: committed L01 implementation as `2ef952a`; clean-checkout target, full verification and worktree check passed. Recorded bounded evidence and retained review status because the specified clean macOS account check has not run. L02 remains pending under dependency discipline.
- 11 September: resumed with Timo's approval to move the fresh-account environment test to G02. Recorded ADR 0003 before revising L01 acceptance; recertifying the approved local scope before consuming the package.
- 11 September: recorded explicit authority to finish the full MVP independently on autopilot. L01 clean-checkout certification passed at `f19d188`, including an uncached native rerun and IC-1 regression; marked L01 done with the fresh-account G02 deferral preserved in evidence.
- 11 September: implemented L02 and passed its local target, including race-tested checkout operations and the previous-head SQLite migration/rollback case. Clean-checkout certification is pending; no later package has consumed L02 yet.
- 11 September: committed L02 implementation as `0c436be`; clean-checkout certification passed and bounded evidence records the filesystem, policy, RPC and migration assertions. Marked L02 done; continuing with L03.
- 11 September: committed L02 certification as `f4b0a2f` and began L03. Current official provider documentation and installed CLI discovery guide the bounded capability experiment; help output is not acceptance proof.
- 11 September: implemented L03 contracts and passed local race-tested fake lifecycle, injection, identity replacement, setup approval/concurrency and actual crash-recovery tests. Atomic publication now retains an edit made after the final hash check. The expanded real CLI experiment distinguishes supported, unsupported and unverified cases; clean certification is next.
- 11 September: committed L03 implementation as `367ede9` and passed clean-checkout certification, including both live CLI probes. Recorded bounded capability/contract evidence and marked L03 done. Continuing with C06 runner identity, grants and proof-of-possession tokens.
- 12 September: implemented C06 and passed its local target, real three-Worker races, key/workspace isolation and rate limits, browser user-verification/step-up negatives, and the full 15-scenario browser regression. Reproduced and fixed a stale named-human launch grant surviving membership removal/rejoin. The server contract deliberately leaves real Keychain keys, live socket delivery and runner management UI to L08/W02; clean-checkout certification is next.
- 12 September: committed C06 implementation as `0247574`; its clean-checkout target, full verification, 15-browser regression and worktree check passed. Recorded bounded security/race evidence and marked C06 done. Continuing with L08 native enrollment, signed-component Keychain access and isolated runner connections.
- 12 September: committed C06 evidence as `48f925b` and began L08. Native credential and TLS possession tests pass locally. The new channel/inventory/durable-reference contracts and migrations are under implementation; no L08 completion or running-MVP claim is made.
- 12 September: wired daemon-owned runner services, local enrollment/status/wake/forget operations, browser approval and sanitized inventory. Real channel testing reproduced a shared bootstrap-limit collision; recurring request challenges now have separate bounded IP/enrollment budgets and heartbeat replies no longer duplicate recovery pulls. Two-workspace native recovery passes through forced launchd restart. Terminal revocation notification handling and remaining certification checks are still in progress.
- 12 September: fixed the HTTP/socket revocation race with an independent authenticated-fence reader and durable terminal cleanup. The full L08 target passes locally (159 protocol tests, 59 focused TypeScript tests, native race/ACL/launchd/Worker tests and browser pairing). `pnpm verify` passes with 436 TypeScript tests and Go/Swift checks. Both D1 and previous-head local migration rollback/recovery checks pass; clean-checkout certification is next.
- 12 September: committed L08 implementation as `9ced7a7`. Clean L08, repository and C06 gates pass. IC-1 exposed hard-coded OAuth browser-test origins when using the supported alternate test port; the test now derives the actual signed-in app origin. Full clean certification is being repeated with that correction.
- 12 September: committed the OAuth test-origin correction as `18b7e20`; full clean L08 certification and all affected regression gates pass. Recorded bounded evidence and marked L08 done. L04 native app implementation is next; no running-MVP or production claim is made.
- 12 September: implemented L04's native app, generated Swift codec, daemon delivery bridge and repeatable development-signing gate. Protocol/native unit checks and signed locked-session diagnostics pass. The full gate exposed a genuine missing Mac provisioning profile; read-only inspection confirmed no eligible profile locally or in the current Apple Developer team. Available-session tests await an unlocked Mac. L04 remains in progress without completion evidence or dependent work.
- 12 September: resumed with approved Apple development setup. Registered the explicit BFB App ID, its Associated Domains capability, this Mac and a development-only profile; Xcode downloaded the named profile. Corrected Apple's scalar wildcard profile representation and Foundation's private-filesystem alias normalization after reproducing their failures. A managed native run passed with consent-denied outcomes and app relaunch. Repeatability and visual inspection are not certified; the Mac locked again during the repeated lifecycle test. No later package has consumed L04.
- 12 September: committed the provisioning/helper corrections and three-cycle native regression gate as `0cc3cb9`. A clean checkout passed frozen installation, full repository verification (462 TypeScript tests, Go checks and 11 Swift tests), Linux cross-build and the clean-worktree assertion. The current native probe also confirmed the verified helper works through the private filesystem alias and the console remains locked. Exact `pnpm test:l04` certification and visual review remain pending; the partial clean checks do not mark L04 done.
- 12 September: after Timo unlocked the Mac, exact `pnpm test:l04` passed in the clean `0cc3cb9` checkout, including all three relaunch cycles. Reviewed the real menu/status window, offline recovery, scrolling and keyboard focus. Committed the neutral heading, accurate Start runner instruction and general-purpose busy label as `c1653e6`; its clean repository verification and cross-build pass. The explicit Terminal-consent control remained pending without an observed decision. The following native gate failed with `app_delivery_unknown`; temporary bounded tracing proved Terminal opened, then the Apple Event send timed out with `-1712`. All temporary diagnostics were removed and owned test processes closed. Automated access to Terminal/system consent UI is unavailable; human dialog inspection is needed, and the Mac locked again. No completion evidence or downstream implementation is claimed.
- 12 September: Timo reported Terminal access allowed and Terminal started. The unchanged `c1653e6` clean checkout passed exact `pnpm test:l04`, now including an actual synthetic fixed-UUID helper receipt and four Terminal acknowledgements across the initial open and three relaunch cycles. Notification denial, malicious-link/socket/signature negatives and independent daemon/child/storage survival pass. Recorded bounded evidence and marked L04 done; C09 durable launch orchestration is next. This completes the milestone packages, not the running full MVP or release certification.
- 12 September: committed C09 implementation as `9b92ef7`. Its exact gate and full repository verification pass in a clean checkout, as do C01 and populated/empty D1 regression gates. C07 exposed a stale exported migration-head constant despite successful D1 migration; corrected the constant and added a manifest-drift assertion. Clean certification is being repeated before C09 can be marked done.
- 12 September: committed the migration-head correction as `e22e2cf`; the complete clean certification sequence passes, including all 16 browser scenarios and the dedicated two-scenario OAuth rerun. Recorded bounded C09 evidence and marked the package done. Continuing with L05's fake-provider Terminal supervisor; real process/lock and real-provider behavior remain unclaimed.
- 12 September: committed C09 evidence as `c625911`, repeated its exact checkpoint and froze L05's local supervision contract. Beginning with isolated native PTY/process-group proof before connecting durable intents, final online authorization and containment recovery. L05 acceptance remains unverified.
- 12 September: L05's isolated macOS PTY proof passes under the race detector: actual foreground Ctrl-C, a 17-second child surviving parent death with a 15-second observation, escaped-descendant detection, and conditional non-stealing foreground restoration. Kernel identity and conservative process-table tests pass. This is the initial process-control proof, not integrated durable launch, lock, recovery or real Terminal.app acceptance.
- 12 September: L05's durable physical-worktree lock and authenticated private-record tests pass under the race detector. A real supervisor crash leaves its surviving child blocked; explicit recovery requires both process absence and a free native lock. Corruption, missing markers, filesystem substitution, reused identities and incomplete history fail closed. Concurrent creation and 24-way lock contention pass ten repeated runs after separating exclusive creation from opening the existing inode. Launch gating, durable intents, final authorization and the real Terminal acceptance remain pending integration.
- 12 September: L05's local migration, durable one-time intent, immutable registration and read-only helper database tests pass under the race detector. Sixteen duplicate issuances share one intent/correlation capability; competing Terminal offers and helper registrations have one winner. Expired/confused assignments and identity replacement block. Go independently reproduces C09's snapshot hash and preserves named claim references; separate provider probes retain a fingerprint-bound installation identity. The authenticated RPC/file handoff, gated exec, cloud lifecycle integration and real Terminal acceptance remain unfinished.
- 12 September: L05 integration reproduced a lost-claim-response gap after C09 lease freshness expires. Added a cleanup-only reconciliation contract bound to the existing runner/signing key and winning request key, without new launch authority or lease extension. Exact C09 passes with 254 protocol cases, 75 focused cases and real Worker/D1 reconciliation after Hub eviction; full repository verification passes with 599 TypeScript tests plus Go and Swift checks. Expiry, launch-grant revocation, release acknowledgement loss and superseded fences are covered. Integrated Terminal execution remains unfinished.
- 12 September: the C09 recovery fix at `a2fb6b9` also passes its exact gate from a clean checkout. L05's real signed-peer harness now proves mutual helper/daemon build authentication, durable registration before response, same-process retry and rejection of another helper or alternate/relaxed signing. A strict response-only local assignment carries no paths, and extra RPC fields cannot select a PID. The 267-case protocol gate, exact C09 target, native race tests, signed harness, full repository verification (612 TypeScript tests, Go and 11 Swift tests) and Linux cross-build pass. Authenticated offline assignment files, gated exec and integrated Terminal lifecycle remain pending.
- 12 September: the signed-registration slice at `7921436` also passed exact `pnpm test:l04` and the signed-helper harness from a clean checkout, preserving real Terminal delivery and the app's native lifecycle checks. L05 now publishes immutable authenticated assignment files before replying, and the helper verifies the file against the signed-peer response. Race tests cover concurrent/conflicting publication, bounded lock contention, non-mutating readers, retained evidence versus expired launch authority, corrupt/substituted records, and same-supervisor recovery after a publication failure. Native tests, ten repeated publication/recovery runs, the real signed-helper harness, full repository verification (612 TypeScript tests, Go and 11 Swift tests) and Linux cross-build pass. Local launch metadata, gated exec and integrated Terminal lifecycle remain unfinished.
- 12 September: committed authenticated assignment files as `ca6caa2`. Added local preparation primitives for frozen installation locations, nine scoped BFB environment values and private execution-specific artifact directories outside the checkout, including native ancestor/alias checks and replacement rejection without repair. A synthetic replacement canary reproduced a preflight gap: the changed executable ran its version probe before identity comparison. Source-bound probes now reject changed executable/configuration inputs before any version/health command, and then compare full identity, version and manifest. Exact L03, supervisor/provider race tests, full repository verification (612 TypeScript tests, Go and 11 Swift tests) and Linux cross-build pass. These are verified preparation primitives; the daemon consumer, gated provider execution and complete Terminal lifecycle are still pending integration.
- 12 September: preparation commit `4cda08a` also passed exact L03, supervisor race tests and the real signed-helper harness from a clean checkout. The new two-pipe exec gate passes 11 real-PTY cases: successful fake execution in an exact subproject with nine scoped BFB values and terminal Ctrl-C, denied final authority, failed durable group recording, pre-exec source/directory/lock changes, wrong parent identity and supervisor death before permission. An authenticated retained lock and absent exec canary verify the crash case. Strict framing/deadline tests, non-executing source checks and a pinned cwd descriptor pass. A full verification run exposed a fixed-sleep UI-test race; `d25c4c2` now waits for observed original and repeated delivery acknowledgements without changing app behavior. Exact L02/L03, supervisor race tests, full repository verification (612 TypeScript tests, Go and 11 Swift tests) and Linux cross-build pass. The daemon consumer, CLI/cloud lifecycle, run controls, exact-session continuation and real Terminal certification remain unfinished.
- 12 September: gated execution commit `8333bac` passed exact L02/L03, supervisor race tests, signed-helper authentication, full repository verification and Linux cross-build from a clean checkout. The daemon now derives final C09 requests from immutable local state, pins cleanup ownership before network I/O, rechecks native lock/peer identity afterward, and durably records only the exact signed direct child. Fixed helper callbacks and additive strict RPC fixtures connect these boundaries. Duplicate requests obtain fresh authorization; competing lock identities, stale/confused responses and ownership loss block. Protocol parity (278 cases), supervisor race tests, the signed-helper harness, full repository verification (623 TypeScript tests, Go and 11 Swift tests) and Linux cross-build pass. The launch consumer, running supervisor/observation loop, cloud lease cleanup, controls, exact-session continuation and real Terminal certification remain in progress; L05 is not done.
- 12 September: final/group RPC commit `41406f7` passed exact C09, native supervisor race tests, signed-helper authentication, full repository verification and Linux cross-build from a clean checkout. Launch-consumer integration then reproduced a cleanup gap for commands rejected before any claim: the original reconciliation contract could not distinguish an absent reservation from a lost response. C09 now returns a cleanup-only `never_acquired` receipt for a terminal, never-claimed execution to its assigned authenticated runner/signing key. It returns no fence or execution configuration and never changes another execution's lease. Expiry and revoked-grant regressions, real Worker/Hub eviction and lease-isolation checks, exact C09 (282 protocol cases, 77 focused tests), full verification (629 TypeScript tests, Go and 11 Swift tests) and Linux cross-build pass. Clean verification of this dependency correction precedes further consumer integration.
- 12 September: the C09 cleanup correction at `b15b238` passed exact C09, full verification and Linux cross-build from a clean checkout. L05 now implements a durable launch consumer with four bounded workers, original-claim retries, single-use Terminal offers, authenticated prepared-source restart checks and unstarted lease cleanup. Additive local migration 005 atomically excludes issuance/registration once cleanup begins and preserves a distinct cleanup identity across restart. Focused native race tests cover lost claim/release replies, conflicting cloud receipts, startup ordering, worker bounds, prepared-source replacement before probing, exact checkout policy/provider checks, denial/locked/unknown app results, and fresh non-persisted Git observations. Exact L02, the complete supervisor race suite, signed-helper authentication, full repository verification (629 TypeScript tests, Go and 11 Swift tests) and Linux cross-build pass. Production entry-point wiring, the running supervisor/observation loop, controls, exact-session continuation and real Terminal certification remain unfinished; L05 and the full MVP are not complete.
- 12 September: launch-consumer commit `035b7c0` also passed exact L02, supervisor race tests, signed-helper authentication, full verification and Linux cross-build from a clean checkout. Native provider-start inspection now compares Darwin dynamically validated running code with the authenticated prepared executable/configuration source, separately from signed BFB helper authentication. Real-process tests reject the waiting wrapper, accept the same PID after provider exec, and reject replaced images, wrong process start and ended processes. Historical source checks never execute a probe or extend launch freshness. Exact L03, the full supervisor race suite, signed-helper authentication, full verification (629 TypeScript tests, Go and 11 Swift tests) and Linux cross-build pass. These are verified startup-observation primitives; the running supervisor/observation loop, production wiring, controls, exact-session continuation and real Terminal certification remain unfinished.
- 12 September: native-image commit `5633ee9` passed exact L03, supervisor race tests, signed-helper authentication, full verification and Linux cross-build from a clean checkout. A real crash regression then reproduced unsafe local recovery between child spawn and durable PID registration. A pre-spawn marker now prevents incomplete history from being treated as never-started absence. The helper lifetime loop retains surviving children and escaped-process markers, checks native identity before TERM/KILL escalation, and restores foreground before final reaping; terminal loss cannot skip cleanup. Thirteen gated PTY scenarios now exercise this loop, alongside real child-survival, escape, cancellation, escalation and spawn-crash tests. Exact L02, the full supervisor race suite, signed-helper authentication, full verification (629 TypeScript tests, Go and 11 Swift tests), Linux executable build and portable supervisor-test compilation pass. The signed bootstrap wrapper is implemented but not yet wired into the production entry point; daemon observations/lease integration, run controls, exact-session continuation and real Terminal certification remain pending. L05 and the full MVP are not complete.
- 13 September: observation storage at `ece9e6a` passed clean verification. The new daemon observer distinguishes preparation, a waiting wrapper, an observed provider image, surviving owned children and whole-group absence without backfilling heartbeats. It persists newly noticed descendants independently of event capacity; explicit local recovery proves absence using both histories, even after an ordinary helper release. A recovery-writer integration error was reproduced and corrected, and missing native fences now block without recreation. Focused recovery/inspection tests, the full supervisor race suite, exact L01, signed-helper authentication, full repository verification and Linux executable/test compilation pass. Clean verification of this integration is next; cloud lease maintenance and the complete Terminal launch remain unfinished.
- 13 September: native-observer integration at `ed27781` passed clean verification. Fresh lease maintenance now uses independent bounded workers, original-claim reconciliation, durable monotonic sequences and current native evidence. Lost acknowledgements cannot replay old heartbeats or release a successor's native lock. Ordinary helper shutdown, registered lock-pinned failures, sticky containment, explicit local recovery, event-capacity failure and worker shutdown are covered. A new child-survival regression caught startup observed only by lease inspection being lost across restart; startup capture now commits before the first cloud renewal. Exact L01, the complete supervisor race suite, signed-helper authentication, full repository verification (653 TypeScript tests, Go and 11 Swift tests) and Linux executable/test compilation pass. Clean certification of this slice is next; pre-pin registered cleanup, controls, production wiring and real Terminal acceptance remain unfinished.
- 13 September: fresh lease integration at `008371b` passed clean verification. Pre-pin registered cleanup now closes the final-authorization gate atomically against racing pinning and rechecks original helper absence before sending a request-bound release. A real crashed helper's abandoned native reservation stays unchanged and occupied after cloud settlement; explicit local recovery alone restores its availability. Lost replies/restart, another execution's held lock, event-capacity failure, PID reuse, contradictory receipts and missing/corrupt native recovery evidence are covered. Exact L01, the complete supervisor race suite, signed-helper authentication, full repository verification and Linux executable/test compilation pass. Clean certification is next; controls, production wiring, exact-session continuation and real Terminal acceptance remain unfinished.
- 13 September: pre-pin cleanup at `f1fb905` passed clean verification. The durable control inbox now binds original claims and targets before delivery; migration 008 adds immutable resumed-launch and effect-start checkpoints with one-way state transitions. Concurrent delivery, changed bindings, expiry, containment, migration rollback, lost cloud acknowledgements, restart and independent worker capacity pass focused race tests. The queue only reads metadata and acknowledges recorded outcomes: reclaiming after termination could reject the ended target before acknowledging its valid signal. Exact L01, the full supervisor race suite, signed-helper authentication, full verification (653 TypeScript tests, Go and 11 Swift tests) and Linux executable/test compilation pass. Native control RPC and helper effects, exact Terminal focus, session continuation, production wiring and real Terminal acceptance remain unfinished.
- 13 September: bound signal RPC and the helper control loop are implemented. Fresh native/online checks precede one-way delivery; the native owner alone signals and reaps, with a final expiry check immediately before `killpg`. Duplicate interrupts, stale/confused controls, post-authorization ownership loss, surviving/escaped children, lost replies, restart and TERM/KILL escalation pass native race tests. A failed marker write after a successful signal reports uncertainty and retains occupancy without repeating the action. A storage-query typo and background lease interference in an older gate-only fixture were reproduced and corrected. Exact L01 (333 protocol cases), the full supervisor race suite, signed-helper authentication, full repository verification (680 TypeScript tests, Go and 11 Swift tests) and Linux executable/test compilation pass. These are socket/process proofs, not real Terminal or full L05 certification; exact Terminal focus, resume, production wiring and Terminal acceptance remain pending.
- 13 September: resume tracing exposed that L03 had exact-session discussion continuation but no interactive resume interface. The additive `PlanResume` and adapter contract now require an exact owned binding and a separately enabled interactive-resume capability; they cannot select a new session or fork. Native synthetic resume, unknown-target rejection, malformed bindings, independent capability ceilings, defensive copies and installation revalidation pass the provider race suite. Clean certification at `32f1354` passed exact L03 (333 protocol cases and the complete provider race suite), full verification (680 TypeScript tests, Go and 11 Swift tests), Linux cross-build and the final clean-worktree check. The manifest preserves the original live-provider experiment as historical version-specific evidence, not a rerun. L05 continues after committing this correction's evidence; no real-provider or full-MVP completion is claimed.
- 13 September: L03 evidence is committed at `f5dc95c`. L05 now dispatches one immutable resumed child after prior-native-absence checks and reconstructs only an exact-session plan in the helper. A release fingerprint prevents late descendant history from inheriting old absence evidence. Native/race tests cover early child delivery, lost claims, restart between binding and acceptance, concurrent begin, capability/binding faults, live retained/reused PIDs, enlarged history and observation-before-acknowledgement. One fault fixture incorrectly tried to reopen a completed command; it now models an actually unsettled source, preserving the immutable-state trigger. Exact L03 (333 protocol cases), the full supervisor race suite, signed-helper authentication, full repository verification (680 TypeScript tests, Go and 11 Swift tests) and Linux executable/test compilation pass. Exact Terminal focus, production wiring and real signed Terminal acceptance remain unfinished; this is not complete L05 certification.
- 13 September: exact-tab focus is committed at `42b02fa`, with native TTY/lock/group binding and signed-app per-effect checks; real focus acceptance remains blocked by the locked Mac session. Production wiring now connects the runner consumers, execution service, signed-app bridge and fixed helper commands. Bounded wake redemption only reconnects a bound existing enrollment; replay, revocation, ambiguous replies and invalid identities cannot create launches. Regression tests reproduced and fixed stranded terminal controls for launches with no local assignment. A CLI import cycle caught by full verification was removed by binding execution handlers in the executable. Focused tests, the full native race suite, full repository verification (698 TypeScript tests, Go and 15 Swift tests) and Linux builds pass. These are implementation-checkout results, not clean L05 certification; explicit local-recovery exposure and integrated real Terminal acceptance remain pending.
- 13 September: daemon wiring is committed at `d5eae0c`. The explicit `execution recover` CLI now uses a mutually authenticated signed-helper RPC accepting only a local UUID. A regression reproduced recovery releasing its marker without certifying the daemon's complete retained history; recovery now performs a fresh native check and stores that fingerprint even for a settled source, preserving uncertainty and creating no process events. Focused native recovery/CLI/binary race tests, the real signed-peer matrix, 359 protocol tests with Go parity, full verification (706 TypeScript tests, Go and 15 Swift tests), and Linux builds pass. One negative fixture's expected error category was corrected after the codecs rejected its forbidden reply payload as intended. These are implementation-checkout checks, not complete L05 Terminal acceptance or clean package certification.
- 17 September: C05 human device and CLI credentials are implemented on `muse/c05`. Browser-approved device bootstrap mints one workspace-bound `bfb_cli_` credential per binding with hash-only storage, epoch-fenced per-request checks, revocation before cleanup, and hashed cross-isolate abuse caps; the device token route is disabled so no web session can result. Exact `pnpm test:c05`, full verification, and the clean-checkout gate passed on the final commit and are recorded in the package evidence; the package is done.
- 17 September: E01 event ingestion, projection, and replay is implemented on `muse/e01`. Authenticated runner batches commit each observation at most once against its immutable execution assignment with explicit per-event dispositions, and expose D1 replay plus high-water reads. Exact `pnpm test:e01`, full verification, and the clean-checkout gate pass; bounded evidence is recorded. Status stays `planned` because L06 is not done; nothing downstream may consume E01 as final.
- 17 September: A01 run-scoped local MCP and context is implemented on `muse/a01` with gate and evidence complete, pending E01, L05 and L06. `bfb mcp stdio` verifies peer UID, owned process group, assignment/generation and correlation, serves provisional read-only bootstrap tools, atomically activates on L06's trusted session binding (consumed through the `SessionBindingSource` double), journals policy-permitted offline mutations in local migration `011` with ordered replay through L08, and closes on revocation, execution end, or terminal result. Exact `pnpm test:a01` (21 race-tested Go functions plus the real-binary stdio harness), `pnpm verify`, `pnpm worktree:check`, the Linux cross-build, and the detached clean-checkout gate pass; evidence is committed at `docs/work-packages/evidence/WP-A01/manifest.json`. Status stays `planned` per roadmap discipline; no downstream package may consume it yet.
- 17 September: A03 result submission and acceptance is implemented on `muse/a03` with gate and evidence complete, pending A01 and E01. Explicit `result.submit` binds an immutable versioned submission (summary, bounded generic evidence refs, limitations, Git facts, server-bound config snapshot/hash) and moves task active to review; `result.request_changes` and `result.accept` move review to active and done with reviewer/member/owner scoping and no agent self-review; `result.fail` and `result.cancel` close runs without moving the task. Outdated state is computed on read (superseded, config-changed, evidence-changed) with history never rewritten. Local MCP `bfb_submit_result` (activated only, journaled offline in widened journal migration v12, replayed through L08 rules) and local CLI `run submit` share domain bounds; REST serves submission, review, failure, and cancellation; the task sheet shows result/review state with no artifact rendering. Exact `pnpm test:a03` (24 unit tests, race-tested Go suites, 10 real-Worker D1 checks plus the real-binary CLI harness, browser submit/change/supersede/accept cycle), `pnpm verify`, `pnpm worktree:check`, the Linux cross-build, and the detached clean-checkout gate pass; evidence is committed at `docs/work-packages/evidence/WP-A03/manifest.json`. Status stays `planned` per roadmap discipline; no downstream package may consume it yet.
