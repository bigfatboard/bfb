{
  "package": "C01",
  "command": "pnpm test:c01",
  "outcome": "passed",
  "checks": [
    "idempotent single effect",
    "concurrent monotonic cursors",
    "authority-bound idempotency",
    "injected failure full rollback",
    "delayed concurrent FIFO order",
    "abuse rate-limit buckets",
    "shared per-workspace hub registry across request resolutions",
    "DO-shaped hub client concurrent FIFO cursors",
    "workspace jurisdiction mismatch rejected"
  ]
}
