# ABOUTME: Second V03 Worker isolate bound to the shared external WorkspaceHub.
# ABOUTME: Requests race through a separate script while the hub owns serialization.

name = "bfb-v03-b"
main = "worker.ts"
compatibility_date = "2026-08-08"
compatibility_flags = ["nodejs_compat"]

[[durable_objects.bindings]]
name = "WORKSPACE_HUB"
class_name = "WorkspaceHub"
script_name = "bfb-v03-hub"
